# Build Process: compilazione, linking e loading

Questo progetto usa un Makefile per automatizzare la trasformazione dei file sorgenti C in un eseguibile.

## Struttura della build

Il codice sorgente si trova in:

```text
src/